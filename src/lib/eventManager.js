// // const eventStore = new Map();
// // const eventTypes = new Set();

// export function setupEventListeners(root) {
//   // Object.entries(eventTypes).forEach(([eventType, handlers]) =>
//   //   Object.entries(handlers).forEach(([handlerKey, handlerValue]) =>
//   //     root.addEventListener(eventType, handlers),
//   //   )
//   // );
// }

// // event 를 저장하는데 어떤 event 인지 특정해줘야 함
// // element 가 파라미터로 넘어오니까 해당 element
// export function addEvent(element, eventType, handler) {
//   element.addEventListener(eventType, (e) => {
//     handler(e);
//   });
// }

// export function removeEvent(element, eventType, handler) {}
